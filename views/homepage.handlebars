{{#each posts as |post| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{post.title}} {{post.content}}</h2>
  </div>
  <div class="col-md-6">
    <h3>{{post.user_id}}</h3>
  </div>
</div>
{{/each}}
